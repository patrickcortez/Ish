public static class Program {
    public static func Main(params let[] args) {
        let $a = 10;
        let $b = 5;
        let $result = $a * $b + 2;
        out $result;
        let $msg = ($result > 50) ? "big" : "small";
        out $msg;
    }
}
